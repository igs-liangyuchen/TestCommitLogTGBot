name: Git AI Code Reviewer (Gemini 3)

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 2 # 必須至少設為 2 才能比對前一次提交

      - name: Run Deep AI Review
        env:
          GEMINI_KEY: ${{ secrets.GEMINI_API_KEY }}
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python3 -c "
          import json, os, urllib.request, urllib.parse, subprocess, sys, html, time

          def run_task():
              # 1. 取得 Git 詳細變動 (關鍵升級)
              try:
                  # 取得 commit 訊息
                  log = subprocess.check_output(['git', 'log', '-1', '--pretty=format:%s']).decode('utf-8')
                  # 取得具體的程式碼差異 (限制長度避免爆量)
                  diff = subprocess.check_output(['git', 'diff', 'HEAD^', 'HEAD']).decode('utf-8')[:3000] 
                  # 取得檔案清單統計
                  stat = subprocess.check_output(['git', 'diff', 'HEAD^', 'HEAD', '--stat']).decode('utf-8')
              except:
                  log, diff, stat = '初次提交', '無差異內容', ''

              # 2. 深度提示詞 (Prompt Engineering)
              prompt = f'''你是一位資深程式語言開發工程師。請用簡潔的方式分析並彙整以下 Git 提交內容：
              【提交訊息】: {log}
              【檔案清單】: {stat}
              【程式碼差異 (Diff)】:
              {diff}
              
              請用繁體中文完成：
              1. 請詳閱有改動或新增的的程式碼
              2. 客觀且具體並簡短的指出程式碼中修正了什麼（例如：修復了某個邏輯、優化了效能或更新了介面）。'''

              # 3. 呼叫 Gemini 3 (含重試)
              api_key = os.getenv('GEMINI_KEY')
              url = 'https://generativelanguage.googleapis.com/v1beta/models/gemini-3-flash-preview:generateContent'
              payload = {'contents': [{'parts': [{'text': prompt}]}]}
              headers = {'Content-Type': 'application/json', 'x-goog-api-key': api_key}
              
              ai_msg = ''
              for i in range(3):
                  try:
                      req = urllib.request.Request(url, data=json.dumps(payload).encode(), headers=headers)
                      with urllib.request.urlopen(req) as response:
                          res = json.loads(response.read().decode())
                          ai_msg = res['candidates'][0]['content']['parts'][0]['text']
                          break 
                  except Exception as e:
                      if i < 2: time.sleep(5); continue
                      ai_msg = f'AI 深度分析失敗: {str(e)}'

              # 4. 發送至 Telegram
              tg_token = os.getenv('TG_TOKEN')
              tg_id = os.getenv('TG_ID')
              safe_ai_msg = html.escape(ai_msg)
              final_text = f'<b>🔍 AI 程式碼深度解析</b>\n\n{safe_ai_msg}'
              
              try:
                  tg_url = f'https://api.telegram.org/bot{tg_token}/sendMessage'
                  tg_params = urllib.parse.urlencode({'chat_id': tg_id, 'text': final_text, 'parse_mode': 'HTML'}).encode()
                  urllib.request.urlopen(tg_url, data=tg_params)
              except:
                  sys.exit(1)

          if __name__ == '__main__':
              run_task()
          "