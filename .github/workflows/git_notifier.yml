name: Git AI Notifier (Official Gemini 3 Mode)

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Run Script
        env:
          GEMINI_KEY: ${{ secrets.GEMINI_API_KEY }}
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python3 -c "
          import json, os, urllib.request, urllib.parse, subprocess, sys, html

          def run_task():
              # 1. 取得提交資訊
              try:
                  log = subprocess.check_output(['git', 'log', '-1', '--pretty=format:%s']).decode('utf-8')
              except:
                  log = '無提交訊息'

              # 2. 呼叫 Gemini API (參考官方 Quickstart 格式)
              api_key = os.getenv('GEMINI_KEY')
              url = 'https://generativelanguage.googleapis.com/v1beta/models/gemini-3-flash-preview:generateContent'
              
              payload = {
                  'contents': [{
                      'parts': [{'text': f'你是一位專業助理。請用繁體中文總結這次的 Git Commit：{log}'}]
                  }]
              }
              
              # 教學建議：將 API Key 放進 Header
              headers = {
                  'Content-Type': 'application/json',
                  'x-goog-api-key': api_key
              }
              
              try:
                  req = urllib.request.Request(url, data=json.dumps(payload).encode(), headers=headers)
                  with urllib.request.urlopen(req) as response:
                      res = json.loads(response.read().decode())
                      ai_msg = res['candidates'][0]['content']['parts'][0]['text']
              except Exception as e:
                  error_body = e.read().decode() if hasattr(e, 'read') else str(e)
                  ai_msg = f'AI 處理異常: {error_body}'

              # 3. 發送至 Telegram (HTML 轉義確保穩定)
              tg_token = os.getenv('TG_TOKEN')
              tg_id = os.getenv('TG_ID')
              safe_ai_msg = html.escape(ai_msg)
              final_text = f'<b>🚀 Gemini 3 開發報告</b>\n\n{safe_ai_msg}'
              
              try:
                  tg_url = f'https://api.telegram.org/bot{tg_token}/sendMessage'
                  tg_params = urllib.parse.urlencode({
                      'chat_id': tg_id, 
                      'text': final_text, 
                      'parse_mode': 'HTML'
                  }).encode()
                  urllib.request.urlopen(tg_url, data=tg_params)
                  print('Success!')
              except Exception as e:
                  print(f'TG 傳送失敗: {e}')
                  sys.exit(1)

          run_task()
          "