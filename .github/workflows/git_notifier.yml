name: Git AI Notifier (Stable Final)

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Run Script
        env:
          GEMINI_KEY: ${{ secrets.GEMINI_API_KEY }}
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python3 -c "
          import json, os, urllib.request, urllib.parse, subprocess, sys, html

          def run_task():
              # 1. 取得 Git 資訊
              try:
                  log = subprocess.check_output(['git', 'log', '-1', '--pretty=format:%s']).decode('utf-8')
              except:
                  log = '無提交訊息'

              # 2. 呼叫 Gemini API
              api_key = os.getenv('GEMINI_KEY')
              url = f'https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key={api_key}'
              
              prompt = f'你是一位開發秘書。請根據以下 Git 提交資訊用繁體中文寫一份簡短日報：\n{log}'
              payload = {'contents': [{'parts': [{'text': prompt}]}]}
              
              try:
                  req = urllib.request.Request(url, data=json.dumps(payload).encode(), headers={'Content-Type': 'application/json'})
                  with urllib.request.urlopen(req) as response:
                      res = json.loads(response.read().decode())
                      ai_msg = res['candidates'][0]['content']['parts'][0]['text']
              except Exception as e:
                  error_detail = e.read().decode() if hasattr(e, 'read') else str(e)
                  ai_msg = f'AI 處理失敗: {error_detail}'

              # 3. 發送 TG (切換為 HTML 模式以增加穩定性)
              tg_token = os.getenv('TG_TOKEN')
              tg_id = os.getenv('TG_ID')
              
              # 格式化訊息，對 AI 內容進行 HTML 轉義防止報錯
              safe_ai_msg = html.escape(ai_msg)
              final_text = f'<b>🤖 AI 開發日報</b>\n\n{safe_ai_msg}'
              
              try:
                  tg_url = f'https://api.telegram.org/bot{tg_token}/sendMessage'
                  tg_data = urllib.parse.urlencode({
                      'chat_id': tg_id, 
                      'text': final_text, 
                      'parse_mode': 'HTML'  # 改用 HTML 模式
                  }).encode()
                  
                  urllib.request.urlopen(tg_url, data=tg_data)
                  print('Success: Message sent to Telegram!')
              except Exception as e:
                  # 如果連 HTML 都失敗，印出詳細原因
                  error_body = e.read().decode() if hasattr(e, 'read') else str(e)
                  print(f'Telegram Failed: {error_body}')
                  sys.exit(1)

          run_task()
          "